<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="bleszerd.com.github.wollpaper">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WollpaperApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WollPaper">
        <activity
            android:name="bleszerd.com.github.wollpaper.feature_wallpaper.presentation.wallpaper_list.ui.WallpaperListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".feature_wallpaper.presentation.wallpaper_details.WallpaperDetailsActivity"
            android:exported="true" />
    </application>

</manifest>